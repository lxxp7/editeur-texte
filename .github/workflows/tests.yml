  name : test_push_workflow
  on: push
  jobs:
    job1:
      runs-on: ubuntu-latest
      steps:
          - name : Checkout
            uses : actions/checkout@v3
          - name : Setup
            uses : actions/setup-java@v1
            with :
                  java-version: 20
          - name : Tests
            run : mvn test